
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: todo

data:
  prometheus.yml: |
    global:
      scrape_interval: 15s

    scrape_configs:
      - job_name: "todo-api"
        metrics_path: "/metrics"
        static_configs:
          - targets:
              - "todo-api:3000"